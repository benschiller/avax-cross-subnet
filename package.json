{
  "name": "cross-subnet-agent-coordinator",
  "version": "0.1.0",
  "description": "Avalanche cross-subnet AI agent coordinator with escrow-enforced commission",
  "main": "scripts/run-demo.sh",
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "test:trace": "forge test -vvv",
    "clean": "forge clean && rm -rf node_modules",
    "deploy:local": "bash scripts/deploy-local.sh",
    "bootstrap": "tsx scripts/bootstrap.ts",
    "demo": "bash scripts/run-demo.sh",
    "lint:sol": "forge fmt --check"
  },
  "devDependencies": {
    "dotenv": "^16.4.5",
    "ethers": "^6.13.2",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  },
  "private": true
}
