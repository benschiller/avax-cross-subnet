# Dependencies
node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml

# Environment (never commit keys or RPC configs)
.env
.env.*
!.env.example

# Foundry build artifacts
cache/
out/
broadcast/

# Git submodules are managed separately
lib/

# Hardhat / Truffle
artifacts/
cache_hardhat/
typechain/
typechain-types/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Demo / deployment outputs (contain local contract addresses and keys)
deployed.json
deployments/
recordings/

# Local relayer state (contains private key config)
.icm-relayer-storage/
.relayer.pid
.keys/
icm-relayer-config.json

# Security: never commit credential files
*.pem
*.key
*.p12
*.secret
secrets/
